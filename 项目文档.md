# CS 面试助手 — 计算机专业面试求职平台

## 一、项目背景

### 1.1 痛点分析

计算机专业学生在求职过程中面临以下核心挑战：

- **面试准备缺乏系统性**：面试知识点分散在各大平台（LeetCode、牛客、博客等），缺乏统一的分类和进阶体系，难以形成完整的知识图谱。
- **实战练习机会有限**：尤其是模拟面试环节，多数求职者缺少与真实面试官对话的练习机会，导致临场发挥不佳。
- **简历质量参差不齐**：大量 CS 毕业生的简历存在描述空洞、缺乏量化成果、技术栈表述不清晰等问题，但在投递前难以获得专业反馈。
- **求职过程管理混乱**：同时投递多家公司时，面试时间线、进度状态、反馈记录等容易遗漏，缺乏有效的追踪工具。

### 1.2 项目目标

针对以上痛点，**CS 面试助手**旨在打造一款**一站式、面向计算机专业的面试求职平台**，覆盖从知识储备、实战模拟到简历优化、求职全流程管理的完整链路，帮助 CS 求职者高效备战、从容拿 Offer。

---

## 二、项目介绍

### 2.1 产品定位

CS 面试助手是一款**面向计算机专业学生和初级工程师的面试求职辅助工具**，以 Web 应用形态呈现，支持桌面端和移动端浏览器访问。

### 2.2 核心功能

| 模块 | 功能 | 说明 |
|------|------|------|
| **仪表盘** | 数据总览、每日挑战、学习进度、面试小贴士 | 首页聚合所有模块的关键数据，提供全局视角 |
| **面试题库** | 8 大分类、46+ 精选题目、多维筛选、收藏、笔记 | 涵盖数据结构、网络、OS、数据库、系统设计等核心 CS 知识 |
| **AI 模拟面试** | 技术面 / HR 面 / 行为面试、实时评分、追问机制、雷达图分析 | 本地智能评估引擎，基于关键词匹配和回答质量给出多维度评分 |
| **简历管理** | 分步编辑、实时预览、3 种模板、AI 评分优化、打印导出 | 结构化简历编辑器，支持多模板切换和智能评分建议 |
| **求职管理** | Kanban 看板、面试时间线、统计仪表盘、搜索筛选 | 全流程岗位追踪，从投递到 Offer 的完整漏斗管理 |
| **设置** | 主题切换、数据导出/导入、一键重置 | 支持浅色/深色主题，数据可备份和迁移 |

### 2.3 技术亮点

- **纯前端架构**：无需后端服务器，所有逻辑在浏览器端完成，零部署成本
- **数据持久化**：基于 `localStorage` 的 Zustand persist 中间件，刷新不丢失
- **响应式设计**：适配桌面和移动端，侧边栏可折叠，布局自适应
- **深色模式**：支持浅色/深色/跟随系统三种主题模式
- **本地 AI 评估**：模拟面试评分引擎完全在本地运行，无需调用外部 API

---

## 三、技术实现

### 3.1 技术栈总览

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| **框架** | Next.js (App Router) | 16.2.3 | React 全栈框架，SSR/SSG 支持 |
| **语言** | TypeScript | 5.x | 类型安全 |
| **UI 组件库** | shadcn/ui (base-ui) | 4.2.0 | 26 个高质量 UI 组件 |
| **样式方案** | Tailwind CSS | 4.x | 原子化 CSS，语义化颜色 Token |
| **状态管理** | Zustand | 5.0.12 | 轻量级状态管理 + persist 持久化 |
| **图标库** | Lucide React | 1.8.0 | 统一的线性图标体系 |
| **动画** | Framer Motion | 12.38.0 | 页面过渡和微交互动画 |
| **构建工具** | Turbopack | — | Next.js 内置的高性能打包器 |

### 3.2 项目架构

```
interview-assistant/
├── src/
│   ├── app/                          # Next.js App Router 页面
│   │   ├── layout.tsx                # 根布局（全局侧边栏 + 顶栏）
│   │   ├── page.tsx                  # 首页仪表盘
│   │   ├── globals.css               # 全局样式 + CSS 变量主题
│   │   ├── questions/
│   │   │   ├── page.tsx              # 面试题库主页面
│   │   │   └── [id]/page.tsx         # 题目详情页（动态路由）
│   │   ├── interview/
│   │   │   ├── page.tsx              # 模拟面试选择页
│   │   │   ├── session/page.tsx      # 面试进行页（对话界面）
│   │   │   └── result/page.tsx       # 面试结果页（评分分析）
│   │   ├── resume/page.tsx           # 简历管理页
│   │   ├── jobs/page.tsx             # 求职管理页
│   │   └── settings/page.tsx         # 设置页
│   ├── components/
│   │   ├── layout/
│   │   │   └── app-layout.tsx        # 全局布局（侧边栏 + 顶栏 + 主题切换）
│   │   └── ui/                       # shadcn/ui 组件（26 个）
│   │       ├── button.tsx, card.tsx, tabs.tsx, badge.tsx ...
│   │       ├── sidebar.tsx           # 可折叠侧边栏导航
│   │       ├── dialog.tsx, sheet.tsx # 弹窗和侧滑面板
│   │       └── ...
│   ├── lib/
│   │   ├── utils.ts                  # 工具函数（cn 类名合并）
│   │   ├── data/                     # 静态数据层
│   │   │   ├── questions.ts          # 面试题库（46 题，8 大分类）
│   │   │   └── interview-questions.ts # 模拟面试题库（16 题，3 种类型）
│   │   └── store/                    # Zustand 状态管理
│   │       ├── question-store.ts     # 刷题进度 + 筛选 + 收藏
│   │       ├── interview-store.ts    # 面试状态 + 评分 + 历史
│   │       ├── resume-store.ts       # 简历数据 + 评分 + 模板
│   │       └── job-store.ts          # 岗位 + 面试记录 + 统计
│   └── hooks/
│       └── use-mobile.ts             # 移动端检测 Hook
├── package.json
├── tsconfig.json
├── next.config.ts
├── components.json                   # shadcn/ui 配置
└── postcss.config.mjs
```

### 3.3 路由设计

采用 Next.js App Router 的文件系统路由：

| 路由 | 页面 | 类型 |
|------|------|------|
| `/` | 首页仪表盘 | 静态 |
| `/questions` | 面试题库列表 | 静态 |
| `/questions/[id]` | 题目详情 | 动态 |
| `/interview` | 模拟面试选择 | 静态 |
| `/interview/session` | 面试进行中 | 静态 |
| `/interview/result` | 面试结果 | 静态 |
| `/resume` | 简历管理 | 静态 |
| `/jobs` | 求职管理 | 静态 |
| `/settings` | 设置 | 静态 |

### 3.4 状态管理设计

项目使用 **Zustand** 作为全局状态管理方案，配合 `persist` 中间件实现数据持久化。共设计 4 个独立 Store：

#### 3.4.1 QuestionStore（题库 Store）

```
状态：
├── allQuestions: Question[]          # 全部题目数据
├── filters: QuestionFilters         # 筛选条件（分类/难度/搜索/状态/标签）
├── answerRecords: Record<string, AnswerRecord>  # 答题记录
└── favorites: Set<string>           # 收藏集合

方法：
├── setCategory / setDifficulty / setSearch / setStatus / setTags  # 筛选操作
├── resetFilters                     # 重置筛选
├── markQuestion(questionId, status) # 标记题目状态（已完成/需复习）
├── toggleFavorite(questionId)       # 切换收藏
├── getFilteredQuestions()           # 获取筛选后的题目列表
├── getStats()                       # 获取统计数据
└── getAllTags()                     # 获取所有标签
```

#### 3.4.2 InterviewStore（面试 Store）

```
状态：
├── status: InterviewStatus          # 面试状态（idle/in-progress/completed）
├── interviewType: InterviewType     # 面试类型（technical/hr/behavioral）
├── questions: InterviewQuestion[]   # 当前面试题目列表
├── messages: Message[]              # 对话消息记录
├── currentResult: InterviewResult   # 当前面试结果
└── history: InterviewHistory[]      # 历史面试记录

方法：
├── startInterview(type)             # 开始面试（随机选题）
├── submitAnswer(answer)             # 提交回答（触发评分 + 追问检测）
├── nextQuestion()                   # 下一题
├── useHint()                        # 使用提示
├── endInterview()                   # 结束面试（生成完整报告）
└── resetInterview()                 # 重置
```

#### 3.4.3 ResumeStore（简历 Store）

```
状态：
├── personalInfo: PersonalInfo       # 个人信息
├── educationList: Education[]       # 教育经历
├── workExperienceList: WorkExperience[]  # 工作经历
├── projectExperienceList: ProjectExperience[]  # 项目经历
├── skillCategories: SkillCategory[] # 技能列表
├── resumeScore: ResumeScore         # 简历评分
└── selectedTemplate: string         # 当前模板

方法：
├── updatePersonalInfo / addEducation / updateEducation / removeEducation
├── addWorkExperience / updateWorkExperience / removeWorkExperience
├── addProjectExperience / updateProjectExperience / removeProjectExperience
├── addSkillCategory / updateSkillCategory / removeSkillCategory
├── analyzeResume()                  # AI 简历评分分析
├── setTemplate(templateId)          # 切换模板
└── resetResume()                    # 重置
```

#### 3.4.4 JobStore（求职 Store）

```
状态：
├── jobList: JobApplication[]        # 岗位列表
└── interviewRecords: InterviewRecord[]  # 面试记录

方法：
├── addJob / updateJob / removeJob / updateJobStatus
├── addInterview / updateInterview / removeInterview
├── getStatistics()                  # 获取统计（投递数/面试数/Offer率）
├── searchJobs(query)                # 搜索岗位
├── filterJobsByStatus(status)       # 按状态筛选
└── resetJobs()                      # 重置
```

### 3.5 数据模型

#### 面试题库数据结构

```typescript
interface Question {
  id: string;                // 唯一标识
  title: string;             // 题目标题
  category: Category;        // 分类（8 大类）
  difficulty: Difficulty;    // 难度（easy / medium / hard）
  tags: string[];            // 知识标签
  content: string;           // 题目描述
  answer: string;            // 参考答案
  hints: string[];           // 渐进式提示
  frequency: number;         // 面试出现频率（1-5）
}
```

题库覆盖 **8 大分类**，共 **46 道**精选面试题：

| 分类 | 题目数 | 示例题目 |
|------|--------|---------|
| 数据结构与算法 | 8 | 反转链表、层序遍历、最长递增子序列、快速排序、LRU 缓存 |
| 计算机网络 | 6 | TCP 三次握手、HTTP/HTTPS 区别、DNS 解析、HTTP/2 |
| 操作系统 | 6 | 进程与线程、死锁、虚拟内存、文件系统与 inode |
| 数据库 | 6 | MySQL 索引、事务隔离级别、Redis 数据类型、SQL 优化 |
| 系统设计 | 5 | 短链接系统、秒杀系统、分布式锁、消息队列选型 |
| 编程语言 | 5 | Java HashMap、Python GIL、JS 事件循环、Go Goroutine |
| 前端开发 | 5 | React Hooks、浏览器渲染、Virtual DOM、CSS 布局 |
| 后端开发 | 5 | Spring Boot 自动配置、RESTful API、微服务、JWT |

#### 模拟面试数据结构

```typescript
interface InterviewQuestion {
  id: string;
  type: InterviewType;              // technical / hr / behavioral
  category: string;                 // 子分类
  question: string;                 // 面试问题
  followUps: FollowUp[];            // 追问列表（含触发关键词）
  evaluationCriteria: EvaluationCriteria[];  // 评分维度
}
```

### 3.6 AI 模拟面试评分引擎

模拟面试采用**本地评分算法**，无需调用外部 AI API，核心逻辑如下：

1. **关键词匹配评分**：根据每道题预设的评分维度和关键词列表，检测用户回答中的关键词覆盖率
2. **回答质量评估**：综合回答长度、结构完整性（是否分段、是否使用 STAR 法则等）给出基础分
3. **追问触发机制**：检测回答中的关键词，匹配预设追问条件，自动触发深度追问
4. **多维度评分**：每道题从多个维度独立评分（如技术准确性、表达清晰度、深度等），最终汇总为雷达图
5. **综合报告生成**：面试结束后，基于各维度平均分和面试类型，自动生成总结评价和改进建议

### 3.7 简历评分算法

简历评分基于本地规则引擎，满分 100 分，分 5 个维度：

| 维度 | 满分 | 评分依据 |
|------|------|---------|
| 个人信息 | 20 | 姓名、邮箱、电话、GitHub 等必填项完整度 |
| 教育经历 | 15 | 学校、专业、学历、时间等字段完整度 |
| 工作经历 | 25 | 公司、职位、描述、技术栈的完整性和详细度 |
| 项目经历 | 25 | 项目描述、角色、技术栈、成果的完整度 |
| 技能列表 | 15 | 技能分类数量和每类技能数量 |

评分后自动生成针对性的改进建议。

### 3.8 UI/UX 设计

#### 设计原则

- **Linear 风格克制设计**：冷静的层级关系、强排版、少色彩、高信息密度
- **语义化颜色系统**：使用 CSS 变量 Token（`bg-primary`、`text-muted-foreground`），不使用硬编码颜色值
- **响应式优先**：移动端侧边栏自动收起为 Sheet，内容区域自适应宽度
- **可折叠侧边栏**：支持展开/图标模式切换，最大化内容区域

#### 主题系统

基于 CSS 自定义属性实现浅色/深色双主题：

```css
:root {
  --background: oklch(1 0 0);       /* 浅色背景 */
  --foreground: oklch(0.145 0 0);   /* 浅色前景 */
  --primary: oklch(0.205 0 0);      /* 主色 */
  ...
}

.dark {
  --background: oklch(0.145 0 0);   /* 深色背景 */
  --foreground: oklch(0.985 0 0);   /* 深色前景 */
  --primary: oklch(0.922 0 0);      /* 主色 */
  ...
}
```

#### 组件体系

基于 shadcn/ui（base-ui 版本）构建，共使用 **26 个** UI 组件：

`Button` `Card` `Tabs` `Badge` `Progress` `Separator` `ScrollArea` `Dialog` `Sheet` `Input` `Textarea` `Select` `Label` `Switch` `Checkbox` `RadioGroup` `Table` `Avatar` `DropdownMenu` `Tooltip` `Alert` `Skeleton` `Command` `Popover` `Collapsible` `Sidebar` `Accordion`

---

## 四、快速开始

### 4.1 环境要求

- Node.js >= 18
- npm >= 9

### 4.2 安装与运行

```bash
# 1. 解压项目
unzip interview-assistant.zip
cd interview-assistant

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev

# 4. 打开浏览器访问
# http://localhost:3000
```

### 4.3 构建生产版本

```bash
# 构建
npm run build

# 启动生产服务
npm start
```

### 4.4 可用脚本

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发服务器（Turbopack 热更新） |
| `npm run build` | 构建生产版本 |
| `npm start` | 启动生产服务器 |
| `npm run lint` | 运行 ESLint 代码检查 |

---

## 五、功能详细说明

### 5.1 首页仪表盘

首页聚合展示用户的核心数据和快捷入口：

- **四大指标卡片**：已刷题目数、模拟面试次数、Offer 数、简历评分
- **功能入口**：题库、模拟面试、简历管理、求职管理四大模块一键跳转
- **每日挑战**：基于日期种子算法，每天推荐一道精选题目
- **学习进度**：总体进度条 + 各分类完成度
- **最近动态**：最近的面试记录和岗位投递状态
- **面试小贴士**：STAR 法则、简历量化、系统设计等实用技巧

### 5.2 面试题库

完整的刷题系统，支持多维度筛选和进度追踪：

- **左侧分类导航**：8 大分类，显示各分类题目数量和刷题进度
- **顶部筛选栏**：关键词搜索 + 难度筛选 + 状态筛选 + 重置
- **标签过滤**：横向滚动标签栏，点击标签快速筛选
- **题目列表**：显示标题、难度标签（颜色区分）、面试频率（火焰图标）、知识标签、收藏按钮
- **题目详情弹窗**：题目描述、渐进式提示（逐个展开）、可折叠参考答案、笔记编辑区、状态标记
- **题目详情页**：独立页面展示完整题目信息，支持上/下题导航

### 5.3 AI 模拟面试

三阶段完整模拟面试流程：

**选择阶段**：
- 技术面试（前端/后端/算法/系统设计 4 个方向）
- HR 面试（自我介绍/职业规划/薪资期望/优缺点）
- 行为面试（STAR 法则训练/团队协作/冲突处理/项目挑战）
- 历史面试记录回顾

**面试阶段**：
- 对话式交互界面（面试官 + 用户消息气泡）
- 实时进度条显示当前题目进度
- 提交回答后即时评分反馈
- 智能追问机制（基于关键词触发）
- 提示功能（获取思路引导）
- 右侧评分面板（桌面端显示评分维度和已完成题目列表）

**结果阶段**：
- 总体评分（SVG 圆环展示）
- 能力维度雷达图（纯 SVG 实现）
- 各维度详细评分和反馈
- 综合评价总结
- 每道题的详细评分和改进建议
- 针对性改进建议列表

### 5.4 简历管理

结构化简历编辑器，支持完整的简历生命周期：

- **分步编辑**：5 个步骤（个人信息 → 教育经历 → 工作经历 → 项目经历 → 技能列表）
- **实时预览**：编辑/预览标签切换，实时查看简历效果
- **3 种模板**：经典模板（居中布局）、现代模板（双栏布局）、极简模板（留白风格）
- **AI 优化建议**：点击分析后展示总分、5 维度评分进度条、改进建议列表
- **导出功能**：通过浏览器打印功能导出为 PDF
- **数据管理**：支持重置所有简历数据

### 5.5 求职管理

全流程岗位追踪系统，4 种视图切换：

- **看板视图**：Kanban 风格，4 列（投递中 / 面试中 / 已 Offer / 已拒绝），岗位卡片可操作
- **列表视图**：表格形式展示所有岗位，含状态、薪资、地点、日期等列
- **面试时间线**：按日期分组展示面试记录，显示轮次、时间、面试官、结果
- **统计仪表盘**：4 个统计卡片 + 转化漏斗（面试转化率、Offer 率）+ 最近投递列表
- **岗位管理**：添加/编辑/删除岗位，支持状态流转
- **面试记录**：关联到具体岗位，记录轮次、面试官、结果、笔记

### 5.6 设置

- **主题切换**：浅色 / 深色 / 跟随系统
- **语言设置**：简体中文 / English
- **通知设置**：每日刷题提醒、自动保存开关
- **数据管理**：导出 JSON 备份 / 导入恢复 / 一键清除所有数据

---

## 六、技术细节

### 6.1 数据持久化策略

所有用户数据通过 Zustand 的 `persist` 中间件自动保存到 `localStorage`，刷新页面不丢失。每个 Store 独立存储：

| Store | localStorage Key | 持久化内容 |
|-------|-----------------|-----------|
| QuestionStore | `question-store` | 答题记录、收藏列表 |
| InterviewStore | `interview-store` | 面试历史记录 |
| ResumeStore | `resume-store` | 简历全部数据 |
| JobStore | `job-store` | 岗位列表、面试记录 |

### 6.2 性能优化

- **Turbopack**：Next.js 16 内置的高性能打包器，开发环境冷启动 < 500ms
- **静态生成**：所有页面均为 SSG（Static Site Generation），构建时预渲染
- **组件按需加载**：shadcn/ui 组件以源码形式引入，无运行时依赖
- **Set 数据结构**：收藏列表使用 `Set` 保证 O(1) 查找性能

### 6.3 类型安全

整个项目使用 TypeScript 严格模式，所有数据模型、Store 接口、组件 Props 均有完整的类型定义，通过 `tsc` 类型检查零错误。

### 6.4 多端适配

- **桌面端**（>= 1024px）：侧边栏展开 + 双栏布局
- **平板端**（768px - 1023px）：侧边栏图标模式 + 单栏布局
- **移动端**（< 768px）：侧边栏收起为 Sheet 抽屉 + 全宽内容区

---

## 七、未来规划

| 方向 | 计划内容 |
|------|---------|
| **题库扩展** | 增加更多题目（目标 200+），支持用户自定义题目 |
| **真实 AI 集成** | 接入 LLM API，实现真正的智能面试官对话 |
| **用户系统** | 添加登录注册，支持云端数据同步 |
| **社区功能** | 面试经验分享、题目讨论、匿名面经 |
| **企业合作** | 企业入驻发布真实岗位，模拟面试对接企业题库 |
| **移动端 App** | 基于 React Native 开发原生移动应用 |
| **PWA 支持** | 添加 Service Worker，支持离线使用和桌面快捷方式 |

---

## 八、许可证

本项目为私有项目，仅供学习和个人使用。
